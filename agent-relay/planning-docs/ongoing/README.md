# Ongoing Planning Docs

Active work packages live here.

Each active package keeps its PRD, checklist, phase relay transcript, audits,
and execution evidence in one slug folder:

```text
agent-relay/planning-docs/ongoing/<work-package-slug>/
```

Move the entire folder to `agent-relay/planning-docs/finished/` after Validator
acceptance.
