# Finished Planning Docs

Completed work packages live here after Validator acceptance.

Keep each package intact under its original slug folder so the PRD, checklist,
relay transcript, audits, and execution evidence stay together:

```text
agent-relay/planning-docs/finished/<work-package-slug>/
```
