This file does not define tool availability.

Use it for local conventions and caution notes:

- Prefer visible structure and attached images over abstract labels.
- Use machine context to clarify state, not to replace observation.
- If image and metadata disagree, call out the conflict explicitly.
