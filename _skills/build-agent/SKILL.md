---
name: pattern-detector-build-agent
description: "Use when the user wants to build a new agent, create a workspace, add an AI analyst role, wire up agent tools, or design agent prompts. Examples: \"Build a new agent\", \"Create a workspace for X\", \"Add a new analyst\", \"Wire up agent tools\", \"How do we structure agents?\""
---

# Build Agent Skill Entrypoint

This file must remain named `SKILL.md` because the skill loader discovers skills through that filename.

For the actual agent-building workflow, read:

- `BUILD_AGENT_WORKFLOW.md`

Ledger, the `Financial Analyst Workspace`, is the canonical completed agent template. Build future agents to match Ledger's workspace shape unless Ledger and the architecture docs are intentionally migrated together.
